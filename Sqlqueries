WHERE
  ( ${backend_name:sqlstring} = 'All'
    OR ts.ENDPOINT_BACKEND = ${backend_name:sqlstring} )
  AND
  ( ${environment:sqlstring} = 'All'
    OR LOWER(ts.ENVIRONMENT) = LOWER(${environment:sqlstring}) )
